# To start using it

```bash
cd ~/Development/lister-app && pnpm install && pnpm build
npm link ./packages/cli
lister setup && lister skill install && lister open
```

# To update and restart later

```bash
./build_run.sh
```
